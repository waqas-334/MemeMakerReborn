package com.androidbull.meme.maker.helper


interface TextColorSelectedListener {
    fun onTextColorSelected(color: Int)
}

interface StrokeColorSelectedListener {
    fun onStrokeColorSelected(color: Int)
}
